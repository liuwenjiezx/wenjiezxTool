# wenjiezxTool — 个人 ComfyUI 功能工具箱（中文友好）

> ComfyUI 自定义节点插件 · 全中文参数 · 共 18 个节点
> 适配 Qwen-Image-2.1 与 KREA2 Turbo 文生图片编辑二合一工作流 · Windows 版 ComfyUI（Python 3.12 / 3.13）

## 功能亮点

- 🚀 **残差缓存加速器（WJZ_TeaCache）**：跳过冗余计算步，提速 30%~60%，质量无损
- 🎛️ **文生图/编辑·画布三合一（WJZ_ModeLatentCanvas）**：模式 + 提示词 + 画面比例 + 图片大小一站式控制
- ✏️ **Krea2 编辑三件套**：源图注入 / 接地指令编码 / 负面提示词强化（跑 KREA2 编辑不用装任何第三方插件）
- ⚡ **SAGE 注意力加速（WJZ_SageAttentionPatch）**：长序列（大图/视频）更快更省显存
- 💬 **全中文参数**：不用记英文

## 节点清单

| 节点 | 功能 |
| --- | --- |
| 随机种子生成器 | 随机种子 / 固定种子复现 |
| 提示词拼接器 | 多段提示词合并 |
| 多行文本拆分器 | 按行拆分，配合批量 |
| 时间戳文件名生成器 | 生成不覆盖的文件名 |
| 图片基本信息查看器 | 输出宽/高/帧数 |
| 数值列表生成器 | 批量种子/强度 |
| 张量形状查看器 | 调试显存/尺寸 |
| 模式选择器 | 文生图/编辑切换（布尔值+索引） |
| 模式文本切换器 | 模式二选一 + 单一文本框 |
| 可选图像加载器 | 文生图自动占位不用放图；图片编辑下拉/上传选图，随模式联动 |
| 图片批量导入器 | 一个窗口拖入/导入多张图片，缩略图可拖动排序，输出打包批量 |
| 图片批量路由 | 一个输入，输出口可加可减（最多16路），按顺序分图给编码器多图口 |
| 文生图/编辑·画布三合一 | 模式+提示词+比例+大小 一站式 |
| 残差缓存加速器 | 跳过冗余步，提速 30%~60% |
| Krea2 编辑·源图注入 | 把参考图插进 Krea2 前向，激活编辑能力 |
| Krea2 编辑·接地指令编码 | 编辑指令带着参考图一起过 Qwen3-VL（画面语义） |
| Krea2 负面提示词强化 | (词:-1.0) 负权重语法抑制概念 |
| SAGE 注意力加速 | 把注意力换成 sageattention 实现 |

## 安装

下载后把 `wenjiezxTool` 文件夹放进 `ComfyUI\custom_nodes\`，重启 ComfyUI，节点搜索框输入 `wenjiezx` 即可。卸载：删除整个文件夹。

## 工作流

### Qwen-Image-2.1文生图片编辑二合一.json

拖入 ComfyUI 即可使用：

- 单 KSampler + If/Else 开关，一次只采样一个分支，省显存
- 三合一节点控制：模式 / 提示词 / 画面比例 / 图片大小 / 编辑分辨率
- **文生图**：不用放任何图片，直接写提示词就能出图（加载器自动占位）\n- **图片编辑**：在「图片批量导入器」窗口拖入/导入多张图片（可拖动排序），「图片批量路由」按顺序分出多路（点按钮加/减路数），接到下方编码器的 image_1/image_2…图片口
- 图片编辑：第 1 张=要改的图，其余=素材图（导入器输出 -> 路由分路 -> 编码器各图片口）
- 参考图大小不影响速度，编码节点会自动缩放

### KREA2Turbo文生图片编辑二合一.json

KREA2 Turbo 速版的文生图 / 图片编辑二合一（同样只装本插件就够）：

- 双 KSampler 各用各的参数（文生图 8 步 beta + 负面强化；编辑 10 步 simple + identity_edit LoRA + 接地指令编码），采样完在 LATENT 上二选一
- If/Else 开关惰性求值，**没选中的分支完全不执行**，不浪费算力
- 左侧三合一节点控制：模式 / 提示词 / 画面比例 / 图片大小 / 张数；编辑模式的出图大小跟画布比例走
- 图片编辑：加载图像节点放参考图，编辑指令写进同一个提示词框即可

## 版本记录

- **v4.3**：新增「图片批量导入器」（多图窗口·拖入/导入/排序）与「图片批量路由」（一进多出·输出口可加减），支持多张参考图编辑
- **v4.2**：新增「可选图像加载器」，文生图自动占位不用放图；图片编辑可直接下拉选图/上传/拖图，随三合一模式自动联动
- **v4.1**：随包新增 **KREA2 Turbo 文生图片编辑二合一** 工作流；补齐 Python 3.13 版 .pyd（此前 cp313 缺 Krea2 三件套与 SAGE 节点，3.13 用户跑不了 KREA2）
- **v4.0**：收编 Krea2 编辑三件套 + SAGE 注意力，跑 KREA2 只装本插件就够
- **v3.1.1**：双版本兼容（Python 3.12 / 3.13 自动适配），最新版 Windows ComfyUI 开箱即用
- **v3.1**：三合一节点人话下拉；新增「编辑分辨率」输出
- **v3.0**：新增三合一节点（合并模式切换+分辨率+空Latent）
- **v2.x**：新增残差缓存加速器
- **v1.x**：首批工具节点

© 2026 wenjiezx
